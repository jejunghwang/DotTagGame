# DotTagGame
Application Software Team Project

키를 아무것도 누르지 않아도 타이머 때문에 모션 업데이트가 계속해서 발생. (제자리에서 뛰는 중)
서버에서 로그인 패킷을 응답하였지만 클라이언트에서 반응이 없을 때가 존재.
클라이언트가 종료하였지만 서버와의 연결이 끊기지 않는 경우가 존재. (메인 폼에서는 연결이 끊기거나, 연결이 유지된다면 start를 눌렀을때 로그인 단계를 건너 뛰어야함. 그렇지만 언제는 정상적으로 연결이 종료)
서버에서 아이디, 비밀번호 양식 체크해야함.
연결 종료할 때 session error 뜨는 이유 확인.
동일 ip접속 거부 구현.
